---
import Navigation from './Navigation.astro';
---

<header class="home-hero">
  <Navigation />

  <div class="section-container hero-shell">
    <div class="hero-grid">
      <div class="hero-content">
        <h1>Automation without the bloat. ROI without the gamble.</h1>
        <p>
          Kyros plugs into the way your team already works—mapping bottlenecks, shipping
          bespoke automations, and leaving you with the documentation to stay in control.
        </p>
        <div class="hero-cta">
          <a class="cta-primary" href="/discovery">Book a discovery sprint</a>
          <a class="cta-secondary" href="/case-studies">See how teams use Kyros</a>
        </div>
      </div>

      <div class="hero-visual">
        <div class="hero-images">
          <div class="hero-image left-angled">
            <img src="/workflow-dashboard.png?v=2" alt="Workflow dashboard" />
          </div>
          <div class="hero-image right-angled">
            <img src="/reporting-hub.png?v=2" alt="Reporting hub" />
          </div>
          <div class="hero-image center-overlay">
            <img src="/centre-img.png?v=2" alt="Automation highlights" />
          </div>
        </div>
      </div>
    </div>
  </div>
</header>

<style>
  .home-hero {
    background: linear-gradient(135deg, #f8f9fa 0%, #ffffff 100%);
    display: flex;
    flex-direction: column;
    gap: 0;
  }

  .hero-shell {
    padding: 4.5rem 0 4rem;
  }

  .hero-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) minmax(0, 1fr);
    gap: 3rem;
    align-items: center;
  }

  .hero-content h1 {
    font-size: clamp(2.2rem, 4vw, 3.6rem);
    font-weight: 700;
    color: #033463;
    margin-bottom: 1.25rem;
    line-height: 1.15;
  }

  .hero-content p {
    color: #475569;
    font-size: 1.05rem;
    line-height: 1.7;
    margin-bottom: 1.75rem;
    max-width: 520px;
  }

  .hero-cta {
    display: flex;
    flex-wrap: wrap;
    gap: 1rem;
  }

  .cta-primary,
  .cta-secondary {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    border-radius: 999px;
    font-weight: 600;
    padding: 0.75rem 1.6rem;
    text-decoration: none;
    transition: transform 0.2s ease, box-shadow 0.2s ease;
  }

  .cta-primary {
    background: linear-gradient(120deg, var(--accent-blue), var(--electric-blue-dark));
    color: #fff;
    box-shadow: 0 15px 30px rgba(13, 110, 253, 0.2);
  }

  .cta-secondary {
    border: 1px solid rgba(3, 52, 99, 0.2);
    color: #033463;
    background: rgba(3, 52, 99, 0.04);
  }

  .cta-primary:hover,
  .cta-secondary:hover {
    transform: translateY(-2px);
  }

  .hero-visual {
    display: flex;
    justify-content: center;
  }

  .hero-images {
    position: relative;
    display: grid;
    grid-template-columns: repeat(2, minmax(0, 1fr));
    gap: 1.5rem;
    align-items: center;
    justify-items: center;
  }

  .hero-image {
    width: clamp(180px, 32vw, 260px);
    border-radius: 18px;
    overflow: hidden;
    box-shadow: 0 24px 50px rgba(3, 52, 99, 0.2);
    background: #ffffff;
  }

  .hero-image img {
    width: 100%;
    height: auto;
    display: block;
  }

  .hero-image.left-angled {
    transform: perspective(900px) rotateY(14deg) rotateX(4deg);
    opacity: 0.9;
  }

  .hero-image.right-angled {
    transform: perspective(900px) rotateY(-14deg) rotateX(-4deg);
    opacity: 0.9;
  }

  .hero-image.center-overlay {
    grid-column: 1 / -1;
    width: clamp(220px, 38vw, 320px);
    margin-top: -2.5rem;
    transform: translateZ(0);
    box-shadow: 0 24px 60px rgba(3, 52, 99, 0.25);
  }

  @media (max-width: 1024px) {
    .hero-grid {
      grid-template-columns: 1fr;
      text-align: center;
      gap: 2.5rem;
    }

    .hero-content p {
      margin-inline: auto;
    }

    .hero-cta {
      justify-content: center;
    }

    .hero-images {
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 1rem;
    }

    .hero-image.left-angled,
    .hero-image.right-angled {
      transform: none;
    }

    .hero-image.center-overlay {
      margin-top: -1rem;
      width: clamp(220px, 60vw, 340px);
    }
  }

  @media (max-width: 620px) {
    .hero-shell {
      padding: 3.5rem 0 3rem;
    }

    .hero-images {
      grid-template-columns: 1fr;
    }

    .hero-image {
      width: min(85vw, 320px);
    }

    .hero-image.center-overlay {
      margin-top: 0;
    }
  }
</style>
